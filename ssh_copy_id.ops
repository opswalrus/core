params:
  user: string?
  host: string?
  identity_file: string?

...

user = params.user || env.USER || "root"
host = params.host || env.HOST
identity_file = params.identity_file || env.SSH_ID || "~/.ssh/id_ed25519.pub"

shell { 'ssh-copy-id -i {{ identity_file }} {{ user }}@{{ host }}' }

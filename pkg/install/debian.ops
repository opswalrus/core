params:
  package: string | array string
...

if params.package.is_a? Array
  shell("install {{ params.package.join(', ') }}") { 'sudo apt install -yq {{ params.package.join(" ") }}' }
else
  shell("install {{ params.package }}") { 'sudo apt install -yq {{ params.package }}' }
end

params:
  package: string

imports:
  os: ../../os
...

case os()
in {base_os: /arch/}
  puts "remove arch"
in {base_os: /debian|ubuntu/}
  debian(package: params.package)
in {base_os: /fedora/}
  puts "remove fedora"
else
  puts "unknown"
end
